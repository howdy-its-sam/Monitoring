{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "rootDirectory": "/",
    "builder": "NIXPACKS",
    "watchPatterns": ["reddit/**", "nixpacks.toml"]
  },
  "deploy": {
    "startCommand": "cd reddit && bun run src/server.ts"
  }
}